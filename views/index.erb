<html>
  <head>
    <title>#blackscreen links</title>
    <style>
      td {
        background-color: #DDD;
      }
    </style>
  </head>
  <body>
    <h1>#randonom links</h1>
    <h3>courtesy of slurp</h3>
    <% unless @links %>
      Currently no links, you should post some in the chat.
    <% end %>
    <table>
    <% @links.each do |link| %>
      <tr><td><%= link.user %></td><td><a href="<%= link.url %>"><%= link.sanitized_title %></a></td><td><%= link.message %></td><td> <%= link.date_added %></td></tr>
    <% end %>
    </table>
  </body>
</html>
